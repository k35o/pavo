{
  "type": "object",
  "required": ["body", "resolve_thread"],
  "additionalProperties": false,
  "properties": {
    "body": { "type": "string", "description": "スレッドへの返信本文（Markdown）" },
    "resolve_thread": { "type": "boolean" },
    "remember": { "type": "string" }
  }
}
